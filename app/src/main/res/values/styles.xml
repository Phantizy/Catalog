<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application Theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#1232CD</item>
        <item name="colorAccent">#03B0FF</item>

        <item name="android:windowAnimationStyle">@style/CatalogAnimation.Activity</item>
    </style>

    <!-- Device must have animation turned on for these to work -->
    <style name="CatalogAnimation.Activity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_open_enter_anim</item>
        <item name="android:activityOpenExitAnimation">@anim/activity_open_exit_anim</item>
        <item name="android:activityCloseEnterAnimation">@anim/activity_close_enter_anim</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_close_exit_anim</item>
    </style>

    <style name="AppTheme.ProductPage" parent="Theme.AppCompat">
        <item name="android:layout_margin">12dp</item>
    </style>

    <style name="AppTheme.ProductPageTitle" parent="Theme.AppCompat">
        <item name="android:textColor">#A50EDD</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.SearchResult.Title</item>
    </style>

    <style name="AppTheme.ProductPagePrice" parent="Theme.AppCompat">
        <item name="android:textColor">#000</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Light.SearchResult.Subtitle</item>
    </style>

    <style name="AppTheme.ProductPageDescription" parent="Theme.AppCompat">
        <item name="android:textColor">#000</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Body1</item>
    </style>

    <style name="AppTheme.ProductPageImage" parent="Theme.AppCompat">
        <item name="android:layout_margin">10dp</item>
    </style>
</resources>